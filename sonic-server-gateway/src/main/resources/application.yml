spring:
  main:
    web-application-type: reactive
  version: @project.version@
  application:
    name: @project.artifactId@
    des: "Gateway of ZPUTech Cloud Real Machine Platform"
  profiles:
    active: @project.artifactId@,eureka,logging,user
