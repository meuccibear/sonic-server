spring:
  version: @project.version@
  application:
    name: @project.artifactId@
    des: "Folder of ZPUTech Cloud Real Machine Platform"
  profiles:
    active: @project.artifactId@,eureka,feign,logging,user
